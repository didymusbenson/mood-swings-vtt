{
  "name": "mood-swings-vtt",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @mood-swings/engine",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm run dev --workspace @mood-swings/app",
    "build": "npm run build --workspaces --if-present"
  }
}
